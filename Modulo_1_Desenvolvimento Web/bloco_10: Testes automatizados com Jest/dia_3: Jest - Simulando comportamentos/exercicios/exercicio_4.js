const caixaAlta = (palavra) => palavra.toUpperCase();

const primeiraLetra = (palavra) => palavra[0];

const concatenarString = (palavra1, palavra2) => palavra1+palavra2;

module.exports = { caixaAlta, primeiraLetra, concatenarString };